function changeFile(file_inp){
	$(file_inp).parent().find("#viewfile").val($(file_inp).val());
}

function clickFile(Obj){
	if($("#jtbox",parent.document).attr("class") == undefined){
		jtload(Obj);
	}
}